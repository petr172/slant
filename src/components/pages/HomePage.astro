---
import Layout from '../../layouts/Layout.astro'
import SanityImage from '../SanityImage.astro'
import TrustWall from '../TrustWall.astro'
import { client } from '../../lib/sanity'
import { ALL_CASE_STUDIES_QUERY, SITE_SETTINGS_QUERY, FEATURED_BLOG_POSTS_QUERY } from '../../lib/queries'
import { useTranslations, type Lang } from '../../i18n/ui'

interface Props { lang: Lang }

const { lang } = Astro.props
const t = useTranslations(lang)
const allProjects = await client.fetch(ALL_CASE_STUDIES_QUERY)
const siteSettings = await client.fetch(SITE_SETTINGS_QUERY)
const blogPosts    = await client.fetch(FEATURED_BLOG_POSTS_QUERY)
const heroMedia: any[] = siteSettings?.heroMedia ?? []

const workUrl      = lang === 'cs' ? '/work'  : '/en/work'
const alternateUrl = lang === 'cs' ? '/en'    : '/'
---

<Layout
  title={t('meta.home.title')}
  description={t('meta.home.desc')}
  {lang}
  {alternateUrl}
>

  <!-- ─── Hero ─────────────────────────────────────────────────────────── -->
  <section class="hero" id="js-hero">

    <!-- Hlavní headline -->
    <div class="hero__headline-wrap container">
      <h1 class="hero__headline" id="js-headline" set:html={t('home.headline')}></h1>
    </div>

    <!-- Video kontejner — clip-path se rozevře při scrollu -->
    <div class="hero__reel-wrap" id="js-reel-wrap">
      <div class="hero__reel" id="js-reel">
        {heroMedia.length > 0 ? heroMedia.map((slide: any, i: number) => (
          <div class={`hero__slide${i === 0 ? ' is-active' : ''}`} data-index={i} data-type={slide._type}>
            {slide._type === 'heroVideo'
              ? <video src={slide.url} autoplay muted playsinline class="hero__media" />
              : slide._type === 'heroExternalImage'
              ? <img src={slide.url} alt={slide.alt ?? ''} class="hero__media" loading={i === 0 ? 'eager' : 'lazy'} />
              : <SanityImage
                  image={slide.image}
                  alt={slide.alt ?? ''}
                  width={1920}
                  height={1080}
                  widths={[800, 1280, 1920]}
                  sizes="100vw"
                  class="hero__media"
                />
            }
          </div>
        )) : (
          <div class="hero__slide is-active" data-type="heroVideo">
            <video autoplay loop muted playsinline class="hero__media">
              <source src="https://videos.pexels.com/video-files/3571264/3571264-uhd_2560_1440_30fps.mp4" type="video/mp4" />
            </video>
          </div>
        )}
        <div class="hero__reel-overlay"></div>
      </div>
    </div>

    <!-- Spodní desc + CTA -->
    <div class="hero__bot container" id="js-hero-bot">
      <p class="hero__desc">{t('home.desc')}</p>
      <a href={workUrl} class="hero__cta">
        {t('home.cta')}
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <path d="M4 10h12M12 6l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>

  </section>

  <TrustWall logos={siteSettings?.clientLogos ?? []} {lang} />

  <!-- ─── Work grid ─────────────────────────────────────────────────────── -->
  {allProjects?.length > 0 && (
    <section class="projects" id="js-projects">
      <div class="container">
        <div class="projects__header">
          <span class="label">{t('home.selected')}</span>
          <a href={workUrl} class="projects__all">{t('work.viewAll')}</a>
        </div>
      </div>

      <div class="container">
      <div class="projects__list">
        {allProjects.map((cs: any, i: number) => {
          const title   = lang === 'en' ? (cs.titleEn   || cs.title)   : cs.title
          const tagline = lang === 'en' ? (cs.taglineEn || cs.tagline) : cs.tagline
          const href    = `${lang === 'cs' ? '/work' : '/en/work'}/${cs.slug}`
          const isLarge = i === 0 || i === 3  // 1. a 4. projekt jsou wide
          return (
            <div class={`project-item ${isLarge ? 'project-item--wide' : ''}`} data-index={i}>
              <a href={href} class="project-item__link">
                <div class="project-item__media">
                  {cs.coverImage
                    ? <SanityImage
                        image={cs.coverImage}
                        alt={cs.coverImage?.alt ?? title}
                        width={isLarge ? 1400 : 900}
                        height={isLarge ? 800 : 650}
                        widths={isLarge ? [700, 1100, 1400] : [450, 700, 900]}
                        sizes={isLarge ? '(max-width: 768px) 100vw, 70vw' : '(max-width: 768px) 100vw, 40vw'}
                        class="project-item__img"
                      />
                    : <div class="project-item__placeholder" />
                  }
                </div>
                <div class="project-item__info">
                  <h2 class="project-item__title">{title}</h2>
                  {tagline && <p class="project-item__tagline">{tagline}</p>}
                </div>
              </a>
              {cs.services?.length > 0 && (
                <div class="project-item__tags">
                  {cs.services.slice(0, 3).map((s: string) => (
                    <a
                      href={`${workUrl}?tag=${encodeURIComponent(s)}`}
                      class="project-item__tag"
                      onclick="event.stopPropagation()"
                    >{s}</a>
                  ))}
                </div>
              )}
            </div>
          )
        })}
      </div>

      <div class="projects__footer">
        <a href={workUrl} class="projects__cta">
          {lang === 'cs' ? 'Všechny projekty' : 'All projects'}
          <svg width="16" height="16" viewBox="0 0 20 20" fill="none">
            <path d="M4 10h12M12 6l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>
      </div>
    </section>
  )}

  <!-- ─── Services ───────────────────────────────────────────────────────── -->
  <section class="services container" id="js-services">
    <h2 class="services__heading">{lang === 'cs' ? 'Co děláme' : 'What we do'}</h2>
    <div class="services__grid">
      {([
        { cs: 'Branding',        en: 'Branding',        tag: 'Branding',        desc_cs: 'Budujeme značky s jasnou osobností — od strategie a pojmenování po vizuální identitu a brand manuál.', desc_en: 'We build brands with clear personality — from strategy and naming to visual identity and brand guidelines.' },
        { cs: 'Webdesign',       en: 'Web design',      tag: 'Webdesign',       desc_cs: 'Navrhujeme weby, které prodávají. UX výzkum, prototypy, finální design a předání developerům.', desc_en: 'We design websites that convert. UX research, prototypes, final design and developer handoff.' },
        { cs: 'Obalový design',  en: 'Packaging',       tag: 'Obalový design',  desc_cs: 'Obal je první dotek zákazníka se značkou. Navrhujeme packaging, který na regále nepřehlédnete.', desc_en: 'Packaging is the first touchpoint. We design packaging that stands out on the shelf.' },
        { cs: 'Grafický design', en: 'Graphic design',  tag: 'Grafický design', desc_cs: 'Tiskové materiály, infografiky, prezentace a veškerý grafický obsah konzistentní s vaší značkou.', desc_en: 'Print materials, infographics, presentations and all visual content consistent with your brand.' },
        { cs: 'Animace',         en: 'Motion design',   tag: 'Animace',         desc_cs: 'Pohyb dává značkám život. Vytváříme animace pro sociální sítě, pitch decky i produktová videa.', desc_en: 'Motion gives brands life. We create animations for social media, pitch decks and product videos.' },
        { cs: 'Naming',          en: 'Naming',          tag: 'Naming',          desc_cs: 'Jméno je základní kámen značky. Pomáháme najít název, který rezonuje, je zapamatovatelný a dostupný.', desc_en: 'A name is the cornerstone of a brand. We help find a name that resonates, is memorable and available.' },
      ] as const).map(s => (
        <div class="service-item">
          <a
            href={`${workUrl}?tag=${encodeURIComponent(s.tag)}`}
            class="project-item__tag service-item__tag"
          >{lang === 'en' ? s.en : s.cs}</a>
          <p class="service-item__desc">{lang === 'en' ? s.desc_en : s.desc_cs}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- ─── Process ────────────────────────────────────────────────────── -->
  {(siteSettings?.processSteps?.length > 0) && (() => {
    const steps: any[] = siteSettings.processSteps
    return (
      <section class="process" id="js-process">
        <div class="process__label container">
          <span class="label">{lang === 'cs' ? 'Jak to děláme' : 'How we work'}</span>
        </div>
        <div class="process__list">
          {steps.map((step: any, i: number) => {
            const title = lang === 'en' ? (step.titleEn || step.title) : step.title
            const desc  = lang === 'en' ? (step.descriptionEn || step.description) : step.description
            const num   = String(i + 1).padStart(2, '0')
            return (
              <div class="process__row container" data-process-row>
                <span class="process__num" aria-hidden="true">{num}</span>
                <div class="process__content">
                  <h3 class="process__title">{title}</h3>
                  <p class="process__desc">{desc}</p>
                </div>
              </div>
            )
          })}
        </div>
      </section>
    )
  })()}

  <!-- ─── Blog ────────────────────────────────────────────────────────── -->
  {blogPosts?.length > 0 && (
    <section class="blog-section" id="js-blog">
      <div class="blog-section__head container">
        <span class="label">{lang === 'cs' ? 'Ze studia' : 'From the studio'}</span>
      </div>

      <div class="blog-scroll" id="js-blog-scroll">
        <div class="blog-track">
          {blogPosts.map((post: any) => {
            const title = lang === 'en' ? (post.titleEn || post.title) : post.title
            const href  = lang === 'cs' ? `/blog/${post.slug}` : `/en/blog/${post.slug}`
            const date  = post.publishedAt
              ? new Date(post.publishedAt).toLocaleDateString(lang === 'cs' ? 'cs-CZ' : 'en-GB', { day: 'numeric', month: 'numeric', year: 'numeric' })
              : ''
            return (
              <a href={href} class="blog-card">
                <div class="blog-card__image">
                  {post.coverImage
                    ? <SanityImage image={post.coverImage} alt={post.coverImage?.alt ?? title} width={600} height={750} widths={[400, 600]} sizes="30vw" />
                    : <div class="blog-card__placeholder" />
                  }
                </div>
                <div class="blog-card__body">
                  {post.category && (
                    <div class="blog-card__tags">
                      <span class="blog-card__tag">{post.category}</span>
                    </div>
                  )}
                  {date && <time class="blog-card__date">{date}</time>}
                  <h3 class="blog-card__title">{title}</h3>
                </div>
              </a>
            )
          })}
        </div>
      </div>

      <div class="blog-section__footer container">
        <a href={lang === 'cs' ? '/blog' : '/en/blog'} class="blog-section__btn">
          {lang === 'cs' ? 'Všechny články' : 'All articles'}
        </a>
        <div class="blog-section__arrows">
          <button id="js-blog-prev" class="blog-arrow" aria-label="Předchozí">
            <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
              <path d="M16 10H4M8 14l-4-4 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
          <button id="js-blog-next" class="blog-arrow" aria-label="Další">
            <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
              <path d="M4 10h12M12 6l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
        </div>
      </div>
    </section>
  )}

  <!-- ─── CTA brutal ──────────────────────────────────────────────────── -->
  <section class="cta-brutal" id="contact">
    <div class="container">
      <div class="cta-brutal__card" id="js-cta-blob">
        <!-- Shape: 6 tilted pills ordered left→right for stagger -->
        <svg class="cta-brutal__svg" viewBox="0 0 1410 471" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" preserveAspectRatio="xMidYMid meet">
          <rect class="cta-pill" width="220" height="400" rx="110" transform="matrix(0.847001 0.531591 -0.469059 0.883167 187.624 0)" fill="var(--clr-accent)"/>
          <rect class="cta-pill" width="220" height="400" rx="110" transform="matrix(0.847001 0.531591 -0.469059 0.883167 394.817 0)" fill="var(--clr-accent)"/>
          <rect class="cta-pill" width="220" height="400" rx="110" transform="matrix(0.847001 0.531591 -0.469059 0.883167 602.011 0)" fill="var(--clr-accent)"/>
          <rect class="cta-pill" width="220" height="400" rx="110" transform="matrix(0.847001 0.531591 -0.469059 0.883167 809.205 0)" fill="var(--clr-accent)"/>
          <rect class="cta-pill" width="220" height="400" rx="110" transform="matrix(0.847001 0.531591 -0.469059 0.883167 1016.4 0)" fill="var(--clr-accent)"/>
          <rect class="cta-pill" width="220" height="400" rx="110" transform="matrix(0.847001 0.531591 -0.469059 0.883167 1223.59 0)" fill="var(--clr-accent)"/>
        </svg>
        <!-- Content overlay -->
        <div class="cta-brutal__content">
          <p class="cta-brutal__text">
            {lang === 'cs'
              ? 'Mám projekt a chci se nezávazně potkat.'
              : 'I have a project and want to meet up.'}
          </p>
          <button class="cta-brutal__btn" data-open-modal onclick="window.openModal?.()">
            {lang === 'cs' ? 'Napište nám' : 'Get in touch'}
            <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
              <path d="M4 10h12M12 6l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
        </div>
      </div>
    </div>
  </section>

</Layout>

<script>
  import { gsap } from 'gsap'
  import { ScrollTrigger } from 'gsap/ScrollTrigger'
  import { SplitText } from 'gsap/SplitText'

  gsap.registerPlugin(ScrollTrigger, SplitText)

  // ── Headline SplitText intro ──────────────────────────────────────────
  const headline = document.getElementById('js-headline')
  if (headline) {
    const split = new SplitText(headline, { type: 'words' })
    gsap.from(split.words, {
      y: 100, opacity: 0, duration: 1.2,
      stagger: 0.06, ease: 'power4.out', delay: 0.2,
    })
  }

gsap.from('#js-hero-bot', {
    y: 20, opacity: 0, duration: 0.8, ease: 'power3.out', delay: 0.8,
  })

  // ── Hero slideshow — hard cut, video on ended, images 1s ─────────────
  const heroSlides = document.querySelectorAll<HTMLElement>('.hero__slide')
  if (heroSlides.length > 1) {
    let current = 0

    function next() {
      heroSlides[current].classList.remove('is-active')
      current = (current + 1) % heroSlides.length
      heroSlides[current].classList.add('is-active')
      scheduleNext()
    }

    function scheduleNext() {
      const slide = heroSlides[current]
      const type = slide.dataset.type
      if (type === 'heroVideo') {
        const video = slide.querySelector('video')
        if (video) {
          video.currentTime = 0
          video.play()
          video.addEventListener('ended', next, { once: true })
        } else {
          setTimeout(next, 3000)
        }
      } else {
        setTimeout(next, 1000)
      }
    }

    scheduleNext()
  }

  // ── Reel clip-path scroll animace ─────────────────────────────────────
  // Začíná jako malý zakulacený obdélník uprostřed,
  // při scrollu se rozevírá na celou šířku
  const reelWrap = document.getElementById('js-reel-wrap')
  const reel     = document.getElementById('js-reel')

  if (reelWrap && reel) {
    // Počáteční stav — malé okno uprostřed
    gsap.set(reel, {
      clipPath: 'inset(8% 18% 8% 18% round 24px)',
    })

    // Intro animace — reel se vyjeví
    gsap.to(reel, {
      clipPath: 'inset(4% 10% 4% 10% round 16px)',
      duration: 1.4, ease: 'power4.out', delay: 0.4,
    })

    const isMobile = window.matchMedia('(max-width: 48em)').matches

    if (isMobile) {
      // Mobil: statický rám, žádný pin
      gsap.set(reel, { clipPath: 'inset(0% 0% 0% 0% round 12px)' })
    } else {
      // Desktop: pin + clip-path scroll animace
      ScrollTrigger.create({
        trigger: reelWrap,
        start: 'top top',
        end: '+=120%',
        pin: true,
        pinSpacing: true,
        scrub: 1,
        onUpdate: (self) => {
          const p = self.progress
          const v = gsap.utils.interpolate(4, 0, p)
          const h = gsap.utils.interpolate(10, 0, p)
          const r = gsap.utils.interpolate(16, 0, p)
          gsap.set(reel, {
            clipPath: `inset(${v}% ${h}% ${v}% ${h}% round ${r}px)`,
          })
        },
      })
    }
  }

  // ── Projects stagger ──────────────────────────────────────────────────
  const items = document.querySelectorAll('.project-item')
  items.forEach((item) => {
    gsap.from(item, {
      y: 60, opacity: 0, duration: 0.9, ease: 'power3.out',
      scrollTrigger: { trigger: item, start: 'top 85%' },
    })
  })

  // ── Blog drag scroll ─────────────────────────────────────────────────
  const blogScroll = document.getElementById('js-blog-scroll')
  if (blogScroll) {
    let isDown = false, startX = 0, scrollLeft = 0
    blogScroll.addEventListener('mousedown', e => { isDown = true; startX = e.pageX - blogScroll.offsetLeft; scrollLeft = blogScroll.scrollLeft })
    blogScroll.addEventListener('mouseleave', () => { isDown = false })
    blogScroll.addEventListener('mouseup', () => { isDown = false })
    blogScroll.addEventListener('mousemove', e => {
      if (!isDown) return
      e.preventDefault()
      const x = e.pageX - blogScroll.offsetLeft
      blogScroll.scrollLeft = scrollLeft - (x - startX) * 1.2
    })

    // ── Blog arrow navigation ───────────────────────────────────────────
    const firstCard = blogScroll.querySelector<HTMLElement>('.blog-card')
    const cardWidth = firstCard ? firstCard.offsetWidth + 24 : 300 // 24px gap
    document.getElementById('js-blog-prev')?.addEventListener('click', () => {
      blogScroll.scrollBy({ left: -cardWidth, behavior: 'smooth' })
    })
    document.getElementById('js-blog-next')?.addEventListener('click', () => {
      blogScroll.scrollBy({ left: cardWidth, behavior: 'smooth' })
    })
  }

  // ── Process rows ─────────────────────────────────────────────────────
  document.querySelectorAll<HTMLElement>('[data-process-row]').forEach((row) => {
    const num     = row.querySelector('.process__num')
    const title   = row.querySelector('.process__title')
    const desc    = row.querySelector('.process__desc')

    const tl = gsap.timeline({
      scrollTrigger: { trigger: row, start: 'top 80%', toggleActions: 'play none none none' }
    })

    tl.fromTo(num,   { x: -30, opacity: 0 }, { x: 0, opacity: 1, duration: 1,   ease: 'power4.out' }, 0)
      .fromTo(title, { y: 28,  opacity: 0, clipPath: 'inset(0 0 100% 0)' },
                     { y: 0,   opacity: 1, clipPath: 'inset(0 0 0% 0)',   duration: 0.8, ease: 'power3.out' }, 0.15)
      .fromTo(desc,  { y: 16,  opacity: 0 }, { y: 0,  opacity: 1, duration: 0.7, ease: 'power3.out' }, 0.35)
  })

  // ── Services stagger ─────────────────────────────────────────────────
  const serviceItems = document.querySelectorAll('.service-item')
  if (serviceItems.length) {
    gsap.from('.services__heading', {
      y: 30, opacity: 0, duration: 0.8, ease: 'power3.out',
      scrollTrigger: { trigger: '.services', start: 'top 85%' },
    })
    gsap.from(serviceItems, {
      y: 40, opacity: 0, duration: 0.7, ease: 'power3.out',
      stagger: { amount: 0.5, from: 'start' },
      scrollTrigger: { trigger: '.services__grid', start: 'top 85%' },
    })
  }

  // ── CTA brutal ────────────────────────────────────────────────────────
  const ctaCard = document.getElementById('js-cta-blob')
  const ctaPills = ctaCard?.querySelectorAll<SVGRectElement>('.cta-pill')

  if (ctaCard && ctaPills?.length) {
    // Scroll into view: shapes scale up 10% (0.9 → 1.0), pivot center
    gsap.from(ctaPills, {
      scale: 0.9,
      transformOrigin: '50% 50%',
      duration: 1.1,
      ease: 'expo.out',
      stagger: { amount: 0.25, from: 'start' },
      scrollTrigger: { trigger: ctaCard, start: 'top 88%' },
    })

    // Hover: tilt +10° stagger left→right, pivot bottom edge
    ctaCard.addEventListener('mouseenter', () => {
      gsap.to(ctaPills, {
        rotation: 10,
        transformOrigin: '50% 100%',
        duration: 0.45,
        stagger: { amount: 0.18, from: 'start' },
        ease: 'power2.out',
        overwrite: 'auto',
      })
    })
    ctaCard.addEventListener('mouseleave', () => {
      gsap.to(ctaPills, {
        rotation: 0,
        transformOrigin: '50% 100%',
        duration: 0.5,
        stagger: { amount: 0.12, from: 'start' },
        ease: 'power2.inOut',
        overwrite: 'auto',
      })
    })
  }
</script>

<style>
/* ── Hero ────────────────────────────────────────────────────────────── */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  background: var(--clr-bg);
}

.hero__top {
  display: flex;
  justify-content: space-between;
  padding-top: calc(4rem + var(--sp-md));
  padding-bottom: var(--sp-md);
}

.hero__label {
  font-size: 0.68rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--clr-muted);
}

.hero__headline-wrap {
  flex: 1;
  display: flex;
  align-items: center;
  padding-block: var(--sp-lg);
}

.hero__headline {
  font-family: var(--font-display);
  font-size: clamp(4rem, 11vw, 12rem);
  font-weight: 400;
  line-height: 0.9;
  letter-spacing: -0.04em;
  color: var(--clr-text);
  max-width: 14ch;
}

/* Reel kontejner — sticky při scrollu */
.hero__reel-wrap {
  width: 100%;
  /* Proportional scaling: 16/9 on mobile, capped at 50vh on desktop */
  aspect-ratio: 16 / 9;
  max-height: 50vh;
  position: relative;
  overflow: hidden;
}

.hero__reel {
  position: absolute;
  inset: 0;
  will-change: clip-path;
}

.hero__slide {
  position: absolute;
  inset: 0;
  opacity: 0;
}
.hero__slide.is-active { opacity: 1; }

.hero__media {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.hero__slide :global(picture),
.hero__slide :global(picture img) {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.hero__reel-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to bottom, rgba(0,0,0,0.2) 0%, rgba(0,0,0,0.5) 100%);
}

.hero__reel-label {
  position: absolute;
  bottom: var(--sp-lg);
  right: var(--sp-lg);
  font-size: 0.68rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.5);
}

.hero__bot {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--sp-xl);
  padding-block: var(--sp-xl);
  border-top: 1px solid var(--clr-border);
}

@media (max-width: 48em) {
  .hero__bot { flex-direction: column; align-items: flex-start; }
}

.hero__desc {
  font-size: clamp(0.875rem, 1.2vw, 1rem);
  color: var(--clr-muted);
  line-height: 1.65;
  max-width: 38ch;
}

.hero__cta {
  display: inline-flex; align-items: center; gap: 0.6rem;
  font-size: 0.875rem; letter-spacing: 0.03em;
  white-space: nowrap; flex-shrink: 0;
  padding-bottom: 2px;
  border-bottom: 1px solid var(--clr-border);
  transition: border-color .25s, gap .3s;
}
.hero__cta:hover { border-color: var(--clr-text); gap: 1rem; }

/* ── Projects ────────────────────────────────────────────────────────── */
.projects { padding-block: var(--sp-3xl); }

.projects__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-bottom: var(--sp-xl);
  border-bottom: 1px solid var(--clr-border);
  margin-bottom: var(--sp-xl);
}

.label { font-size: 0.68rem; letter-spacing: 0.12em; text-transform: uppercase; color: var(--clr-muted); }

.projects__all { font-size: 0.8rem; opacity: 0.45; transition: opacity .2s; }
.projects__all:hover { opacity: 1; }

.projects__footer {
  display: flex;
  justify-content: center;
  padding-top: var(--sp-xl);
}

.projects__cta {
  display: inline-flex;
  align-items: center;
  gap: 0.6rem;
  font-size: 0.875rem;
  letter-spacing: 0.03em;
  padding: 0.75em 1.75em;
  border: 1px solid var(--clr-border);
  border-radius: 999px;
  transition: border-color .25s, background .25s, gap .3s;
}
.projects__cta:hover {
  border-color: var(--clr-text);
  background: color-mix(in srgb, var(--clr-text) 5%, transparent);
  gap: 1rem;
}

/* Alternating grid — 2 col s wide variantami */
.projects__list {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--sp-sm);
}

@media (max-width: 56em) {
  .projects__list { grid-template-columns: 1fr; }
}

.project-item {
  position: relative;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  background: var(--clr-surface);
  border-radius: var(--radius-md);
  cursor: pointer;
}

.project-item__link {
  display: block;
  flex: 1;
  text-decoration: none;
  color: inherit;
}

.project-item--wide {
  grid-column: 1 / -1;
}

.project-item__media {
  aspect-ratio: 16 / 9;
  overflow: hidden;
  border-radius: var(--radius-md) var(--radius-md) 0 0;
}

.project-item--wide .project-item__media {
  aspect-ratio: 21 / 9;
}

@media (max-width: 56em) {
  .project-item--wide .project-item__media { aspect-ratio: 16 / 9; }
}

.project-item__media :global(picture) {
  display: block;
  width: 100%;
  height: 100%;
}

.project-item__media :global(picture img),
.project-item__media :global(.project-item__img) {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
}

.project-item:hover .project-item__media :global(picture img),
.project-item:hover .project-item__media :global(.project-item__img) { transform: scale(1.04); }

.project-item__placeholder {
  width: 100%; height: 100%; background: var(--clr-surface);
}

.project-item__info {
  padding: var(--sp-md) var(--sp-lg) var(--sp-sm);
  border-top: 1px solid var(--clr-border);
}

.project-item__title {
  font-family: var(--font-sans);
  font-size: clamp(1.1rem, 1.8vw, 1.5rem);
  font-weight: 400;
  letter-spacing: -0.02em;
  margin-bottom: var(--sp-2xs);
}

.project-item__tagline {
  font-size: 0.875rem;
  color: var(--clr-muted);
  line-height: 1.4;
}

.project-item__tags {
  display: flex;
  flex-wrap: wrap;
  gap: 0.375rem;
  padding: var(--sp-sm) var(--sp-lg) var(--sp-md);
}

.project-item__tag {
  display: inline-block;
  padding: 0.25em 0.65em;
  border-radius: 999px;
  border: 1px solid var(--clr-border);
  font-size: 0.7rem;
  font-weight: 600;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--clr-muted);
  transition: color 0.2s, border-color 0.2s, background 0.2s;
  position: relative;
  z-index: 1;
}
.project-item__tag:hover {
  color: var(--clr-text);
  border-color: var(--clr-text);
  background: color-mix(in srgb, var(--clr-text) 5%, transparent);
}

.project-item__meta {
  display: flex;
  gap: var(--sp-sm);
  font-size: 0.72rem;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--clr-muted);
}

/* ── Process ────────────────────────────────────────────────────────── */
.process {
  padding-block: var(--sp-3xl);
}

.process__label {
  margin-bottom: var(--sp-xl);
}

.process__list {
  display: flex;
  flex-direction: column;
  gap: 0;
}

.process__row {
  display: grid;
  grid-template-columns: 28% 1fr;
  gap: var(--sp-2xl);
  align-items: center;
  padding-block: var(--sp-2xl);
  border-top: 1px solid var(--clr-border);
}

.process__row:last-child {
  border-bottom: 1px solid var(--clr-border);
}

/* Sculpted number — light grey, display weight */
.process__num {
  font-family: var(--font-display);
  font-size: clamp(5rem, 14vw, 12rem);
  font-weight: 700;
  line-height: 0.85;
  letter-spacing: -0.06em;
  color: var(--clr-border);
  user-select: none;
  will-change: transform, opacity;
}

/* Hover: number tints to text colour */
.process__row:hover .process__num {
  color: var(--clr-muted);
  transition: color 0.4s ease;
}

.process__content {
  display: flex;
  flex-direction: column;
  gap: var(--sp-sm);
}

.process__title {
  font-family: var(--font-display);
  font-size: clamp(1.5rem, 3vw, 2.6rem);
  font-weight: 400;
  letter-spacing: -0.03em;
  color: var(--clr-text);
  line-height: 1.05;
}

.process__desc {
  font-size: clamp(0.88rem, 1.05vw, 1rem);
  line-height: 1.75;
  color: var(--clr-muted);
  max-width: 48ch;
}

@media (max-width: 48em) {
  .process__row {
    grid-template-columns: auto 1fr;
    gap: var(--sp-md);
    align-items: start;
  }
  .process__num {
    font-size: clamp(3.5rem, 18vw, 6rem);
  }
}

/* ── Services ───────────────────────────────────────────────────────── */
.services {
  padding-block: var(--sp-3xl);
}

.services__heading {
  font-family: var(--font-display);
  font-size: clamp(2rem, 4vw, 3.5rem);
  font-weight: 400;
  letter-spacing: -0.03em;
  margin-bottom: var(--sp-2xl);
  color: var(--clr-text);
}

.services__grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0;
}

.service-item {
  padding: var(--sp-lg) 0;
  border-top: 1px solid var(--clr-border);
  padding-right: var(--sp-2xl);
}

.service-item:nth-child(even) {
  padding-right: 0;
  padding-left: var(--sp-2xl);
  border-left: 1px solid var(--clr-border);
}

/* last row: also border-bottom */
.service-item:nth-last-child(-n+2) {
  border-bottom: 1px solid var(--clr-border);
}

.service-item__tag {
  margin-bottom: var(--sp-sm);
}

.service-item__desc {
  font-size: clamp(0.9rem, 1.1vw, 1rem);
  line-height: 1.6;
  color: var(--clr-muted);
  max-width: 38ch;
}

@media (max-width: 48em) {
  .services__grid {
    grid-template-columns: 1fr;
  }
  .service-item:nth-child(even) {
    padding-left: 0;
    border-left: none;
  }
  .service-item:nth-last-child(-n+2) {
    border-bottom: none;
  }
  .service-item:last-child {
    border-bottom: 1px solid var(--clr-border);
  }
  .service-item {
    padding-right: 0;
  }
}

/* ── Blog ────────────────────────────────────────────────────────────── */
.blog-section { padding-block: var(--sp-3xl); }

.blog-section__head {
  padding-bottom: var(--sp-lg);
  border-bottom: 1px solid var(--clr-border);
  margin-bottom: var(--sp-xl);
}

/* Horizontal scroll */
.blog-scroll {
  overflow-x: auto;
  overflow-y: visible;
  padding-inline: var(--gutter);
  padding-bottom: var(--sp-lg);
  scrollbar-width: none;
  cursor: grab;
  scroll-snap-type: x mandatory;
}
.blog-scroll::-webkit-scrollbar { display: none; }
.blog-scroll:active { cursor: grabbing; }

.blog-track {
  display: flex;
  gap: var(--sp-md);
  width: max-content;
}

/* Editorial card — no background, image dominant */
.blog-card {
  display: flex;
  flex-direction: column;
  width: clamp(220px, 26vw, 360px);
  flex-shrink: 0;
  text-decoration: none;
  color: inherit;
  scroll-snap-align: start;
}

.blog-card__image {
  aspect-ratio: 4 / 5;
  overflow: hidden;
  border-radius: var(--radius-md);
  background: var(--clr-surface);
  margin-bottom: var(--sp-md);
}
.blog-card__image :global(picture),
.blog-card__image :global(picture img) {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: transform 0.6s cubic-bezier(0.16,1,0.3,1);
}
.blog-card:hover .blog-card__image :global(picture img) { transform: scale(1.04); }

.blog-card__placeholder {
  width: 100%;
  height: 100%;
  background: var(--clr-surface);
}

.blog-card__body {
  display: flex;
  flex-direction: column;
  gap: var(--sp-2xs);
}

.blog-card__tags {
  display: flex;
  gap: 0.375rem;
  margin-bottom: 0.25rem;
}

.blog-card__tag {
  display: inline-block;
  padding: 0.25em 0.65em;
  border-radius: 999px;
  border: 1px solid var(--clr-border);
  font-size: 0.7rem;
  font-weight: 600;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--clr-muted);
}

.blog-card__date {
  font-size: 0.72rem;
  letter-spacing: 0.04em;
  color: var(--clr-muted);
}

.blog-card__title {
  font-family: var(--font-sans);
  font-size: clamp(0.95rem, 1.4vw, 1.15rem);
  font-weight: 500;
  letter-spacing: -0.02em;
  line-height: 1.35;
}

/* Footer: btn left, arrows right */
.blog-section__footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: var(--sp-lg);
}

.blog-section__btn {
  display: inline-flex;
  align-items: center;
  padding: 0.55em 1.25em;
  border-radius: 999px;
  border: 1px solid var(--clr-border);
  font-size: 0.8125rem;
  font-weight: 500;
  color: var(--clr-text);
  transition: border-color .2s, background .2s;
}
.blog-section__btn:hover {
  border-color: var(--clr-text);
  background: color-mix(in srgb, var(--clr-text) 5%, transparent);
}

.blog-section__arrows {
  display: flex;
  gap: 0.5rem;
}

.blog-arrow {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 2.5rem;
  height: 2.5rem;
  border-radius: 999px;
  border: 1px solid var(--clr-border);
  color: var(--clr-text);
  transition: border-color .2s, background .2s;
  cursor: pointer;
}
.blog-arrow:hover {
  border-color: var(--clr-text);
  background: color-mix(in srgb, var(--clr-text) 5%, transparent);
}
.blog-arrow:disabled { opacity: 0.3; cursor: default; }

/* ── Contact ─────────────────────────────────────────────────────────── */
/* ── CTA brutal ──────────────────────────────────────────────────────── */
.cta-brutal {
  padding-block: var(--sp-3xl);
}

.cta-brutal__card {
  position: relative;
  width: 100%;
  /* height driven by the SVG aspect ratio (1410×471 ≈ 3:1) */
}

/* SVG fills container width, height scales proportionally */
.cta-brutal__svg {
  display: block;
  width: 100%;
  height: auto;
}

/* Content floats over the SVG */
.cta-brutal__content {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--sp-xl);
  padding-inline: clamp(2rem, 7vw, 5rem);
}

.cta-brutal__text {
  font-family: var(--font-display);
  font-size: clamp(1.1rem, 3vw, 2.4rem);
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.03em;
  color: #fff;
  max-width: 16ch;
}

.cta-brutal__btn {
  display: inline-flex;
  align-items: center;
  gap: 0.6rem;
  font-size: 0.875rem;
  font-weight: 600;
  letter-spacing: 0.03em;
  white-space: nowrap;
  padding: 0.75em 1.5em;
  border-radius: 999px;
  background: var(--clr-text);
  color: var(--clr-bg);
  border: none;
  flex-shrink: 0;
  cursor: pointer;
  transition: gap 0.3s, background 0.25s, color 0.25s;
}

.cta-brutal__btn:hover {
  gap: 1rem;
  background: #fff;
  color: #111;
}

@media (max-width: 48em) {
  .cta-brutal__content {
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    padding-inline: clamp(1.5rem, 6vw, 2.5rem);
    gap: var(--sp-md);
  }
  .cta-brutal__text {
    font-size: clamp(1rem, 5vw, 1.5rem);
  }
}

.contact { padding-block: var(--sp-3xl); }

.contact__inner {
  border-top: 1px solid var(--clr-border);
  padding-top: var(--sp-xl);
  display: flex; flex-direction: column; gap: var(--sp-lg);
}

.contact__headline {
  font-family: var(--font-display);
  font-size: clamp(3rem, 8vw, 9rem);
  font-weight: 400; line-height: 0.95; letter-spacing: -0.03em;
}

.contact__email {
  display: inline-block;
  font-family: var(--font-display);
  font-size: clamp(1.5rem, 3.5vw, 3.5rem);
  letter-spacing: -0.02em;
  opacity: .6;
  transition: opacity .25s;
  position: relative;
  padding-bottom: 4px;
}
.contact__email::after {
  content: '';
  position: absolute; bottom: 0; left: 0;
  width: 0; height: 1px; background: var(--clr-text);
  transition: width .5s var(--ease-out-expo);
}
.contact__email:hover { opacity: 1; }
.contact__email:hover::after { width: 100%; }
</style>
